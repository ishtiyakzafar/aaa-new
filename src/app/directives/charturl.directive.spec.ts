import { CharturlDirective } from './charturl.directive';

describe('CharturlDirective', () => {
  it('should create an instance', () => {
    // const directive = new CharturlDirective();
    // expect(directive).toBeTruthy();
  });
});
