import { FilterClientCodePipe } from './filter-client-code.pipe';

describe('FilterClientCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClientCodePipe();
    expect(pipe).toBeTruthy();
  });
});
